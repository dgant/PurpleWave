package Information.Battles

object Clustering {
  val clusterDistanceTiles: Int = 16
  val clusterDistancePixels: Int = 32 * clusterDistanceTiles
  val clusterDistanceSquared: Int = clusterDistancePixels * clusterDistancePixels
}
